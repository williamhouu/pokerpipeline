"""poker-pipeline: the 8-layer poker question-generation pipeline.

See docs/engineering_brief.docx for the full architecture. Each layer is built
as a submodule with clear inputs and outputs; `fact_extractor` is Layer 5.
"""
